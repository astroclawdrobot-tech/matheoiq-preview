NODE_ENV=production
PORT=8788
# Preview is safe by default. External writes are intentionally not implemented here.
# Real production must bind a backend with RLS/ownership tests, verified doctor credentials,
# legal/privacy approval, payment test mode, and explicit GO before live dispatch/payment.
